# 完成前ギャップ監査レポート

作成日: 2026-07-07
対象コミット: `109b2f6`（Add Claude project instructions）
監査方法: 全ソースコード読了（5,527行）、GitHub履歴（26コミット）確認、`npx tsc --noEmit` 実行、**Web版アプリを実際に操作した実機検証**（新規人物追加フローの実地テストを含む）

---

## 0. 最初に理解すべき前提（総合判定の前に）

`CLAUDE.md` と本Issueは、Supabase・認証・LLM Adapter・RLSがある程度存在する前提で「完成基準」を定義している。しかし実際のコードベースを全数調査した結果、**Supabase・認証・LLM Adapter・RLSはコード上に一件も存在しない**（`supabase|Supabase`は`CLAUDE.md`と`README.md`の文言以外にヒットなし、`ollama|OpenAI|Claude|Gemini|LLMAdapter`・`login|Auth|signIn`・`user_id|RLS`は`src/`配下で0件）。

README自身も「まだ以下は入れていません：Supabase / 本物のAI API / ログイン / クラウド同期」と明記しており、これは矛盾ではなく正直な現状表示である。

つまり本プロジェクトは、`CLAUDE.md`が定義する**完成状態（フェーズ3〜4：Supabase+Auth+RLS+実AIを備えた複数ユーザーSaaS）**に対して、**フェーズ0（ローカル単体デバイス上のUIプロトタイプ、モックAI、認証なし）**の段階にある。この監査は「フェーズ0の中身がどこまで健全か」と「フェーズ0→完成までに何が必要か」の両方を扱う。

---

## 1. 総合判定

**現状は「完成として出せない」。それどころか、プロトタイプとして内部的にも致命的な機能不全が1件見つかった（下記1.1）。**

さらに重要な点として、`docs/`に存在すべきとされていた以下6ファイルはすべて**存在しない**：
`rc-remediation-report.md` / `mobile-beta-qr-test.md` / `new-contact-integration-audit.md` / `mvp-checklist.md` / `mvp-critical-fixes-report.md` / `current-state-audit.md`
（存在するのは `docs/ai-analysis-contract.md` のみ）

### 1.1 最重要の発見：新規人物を追加する導線が実質的に存在しない（P0）

実機検証で確認した事実:

- 人物追加画面（`AddPersonScreen`）への遷移は、コード全体で **1箇所** (`src/screens/HomeScreen.tsx:213`) のみ。
- その呼び出し元は `PeoplePane` の `ListEmptyComponent`（`src/screens/HomeScreen.tsx:465-473`）、つまり**人脈カードが0件のときだけ表示される「人物を追加する」ボタン**。
- アプリ起動時、`loadPeople()`（`src/screens/HomeScreen.tsx:128-142`）が`MOCK_PEOPLE`（3件）を自動シードし、以後AsyncStorageに永続化される。
- 人物の削除・アーカイブ機能は一切存在しない（`CLAUDE.md` 5.9で要求されているアーカイブ機能も未実装）。
- 結果として**人脈カード一覧が0件になることは通常操作では起こり得ず、「人物を追加する」ボタンは恒久的に非表示**。
- ヘッダー右上の人物追加風アイコン（`UserPlus`）は、`Header`コンポーネントの`onAdd`に統一的にバインドされており、実体は`Alert.alert('今日やることを追加', '...想定です')`という無関係なスタブ（`src/screens/HomeScreen.tsx:196`）。人脈タブでもこのアイコンは人物追加ではなくこのスタブを呼ぶ。
- ブラウザで直接 `http://localhost:8081/AddPerson` にアクセスしても、`NavigationContainer`に`linking`設定がなく、Homeにリダイレクトされることを確認済み。

**つまり、通常のUI操作で新規人物を1人も追加できない。** `CLAUDE.md`の「最上位ルール」であり本Issueの主題でもある「新規人物での動作確認」は、**第一歩（UIからの新規人物追加）の時点で実行不可能**という結果になった。

---

## 2. 完成として出せるか

**出せない。** 上記1.1により、営業マンの最初のオンボーディング行為（新規に会った人を登録する）自体ができない。加えてSupabase・認証・実AI・RLSが不在のため、`CLAUDE.md`の非交渉完成ゲート（10項目中、実質7項目）を満たしていない。

## 3. 営業マンに触らせてよいか

**触らせるべきではない。** 現状で触らせた場合、ユーザーは最初の一歩（新規人物追加）で詰まる。仮に開発者がAsyncStorageを直接操作してテストデータを増やしたとしても、以下が続けて発生する：

- ホームの「今日の優先行動」は山本さん／田中さん／佐藤さんという**固定名の人物のみ**を検索して表示する（`createTodayActions`, `src/screens/HomeScreen.tsx:2363-2406`）。新規人物は絶対にここへ出てこない。
- 「AIで整理する」（`createMockAnalysis`, `src/data/mockAnalysis.ts:3`）は引数`_memo`を**完全に無視**し、常に同一の「田中さん」分析を返す。入力内容は一切反映されない。
- 終業後チェック（`EndOfDayPane`）は`people`を受け取らず、日付・件数・人名すべてが固定文字列（存在しない「村上さん」まで登場）。

---

## 4. 完成までの不足項目一覧（要約）

| 領域 | 状態 |
|---|---|
| 新規人物追加の導線 | **導線が実質死んでいる（P0）** |
| Supabase | 未着手（テーブル0、依存関係0） |
| 認証 | 未着手（ログイン画面0） |
| LLM Adapter | 未着手（すべてUI直書きのローカル正規表現ヒューリスティック） |
| RLS/user_id | 対象となるバックエンドが存在しないため評価不能 |
| 予定前ナビ／後メモ／文面確認／終業後チェック画面 | **二重実装**（`src/screens/*.tsx`は死んだコード、実体は`HomeScreen.tsx`内のPane） |
| 人物アーカイブ・削除 | 未実装 |
| 通知 | ローカル通知のみ実装済み（実機で機能する数少ない完成機能） |
| モバイルQR/Cloudflare Tunnel/betaプロキシ | 該当設定が repo に一切存在しない（`CLAUDE.md`記載のインフラは未構築） |
| 法務／プライバシー／AI注意 | 未着手 |
| テスト／CI | 0件（lint/testスクリプトなし、`.github/workflows`なし） |
| 型チェック | **合格**（`npx tsc --noEmit` エラー0件） |

---

## 5. P0一覧（完成/β継続不可）

1. **新規人物を追加する導線が実質存在しない**（`src/screens/HomeScreen.tsx:465-473`, `:213`）。ListEmptyComponent以外に`AddPerson`への遷移経路がなく、モック3件が常駐するため空リストにならない。
2. **ホームの「今日の優先行動」が山本／田中／佐藤の固定名検索**（`createTodayActions`, `src/screens/HomeScreen.tsx:2363-2406`）。新規人物のタスクが一切表示されない。
3. **「AIで整理する」がユーザー入力を無視する**（`createMockAnalysis`, `src/data/mockAnalysis.ts:3`、引数`_memo`が未使用）。誰の情報を入れても同じ「田中さん」データが返る。
4. **終業後チェックが実データと無接続**（`EndOfDayPane`, `src/screens/HomeScreen.tsx:1496-1767`）。`people`をpropsで受け取らず、日付・件数・存在しない人物名まで固定文字列。
5. Supabase等の永続層が不在（`CLAUDE.md`完成ゲート2〜4・6・7が原理的に評価不能）。

## 6. P1一覧（完成前に必須）

- Supabase導入（`CLAUDE.md` 4.2記載の16テーブル設計・接続）
- 認証導入（サインアップ・ログイン・パスワードリセット・セッション復元）
- LLM Adapter層の新設（UI→logic→Adapter→provider の分離。現状は`HomeScreen.tsx`内に`inferPain`/`inferTemperature`/`createLineCheckAnalysis`等30個近いヘルパー関数が直書き）
- 人物アーカイブ・削除機能（`CLAUDE.md` 5.9で必須とされているが未実装。アーカイブができないと問題1のP0も回避不能）
- `src/screens/PreMeetingNavScreen.tsx` / `AfterMemoScreen.tsx` / `LineCheckScreen.tsx` / `EndOfDayCheckScreen.tsx`（合計513行）と`HomeScreen.tsx`内のPaneの二重実装の解消（バグ温床）
- 新規人物E2Eの手動回帰チェックリストの整備・実施
- 最低限の利用規約・プライバシー・AI注意ページ（実ユーザーに触らせる前提なら必須）

## 7. P2一覧（β後/拡大前）

- HomeScreen.tsx（2,941行の単一ファイル）の分割（保守性リスク）
- `想定です`とラベル付けされた27箇所のAlertスタブの実装（履歴閲覧、参照情報編集、通知確認、明日への反映編集など）
- PersonCardの「詳細/LINE文/通知」ミニアクション、Homeの「詳細/完了/延期」ミニボタンが`View`のみで`onPress`なし（装飾のみ、押しても無反応）→実装または削除
- 営業コーチのマルチターン化・`coach_logs`相当の履歴保持
- モバイルQR/Cloudflare Tunnel/betaプロキシ環境の構築（現状repoに存在しない）
- Lint・CI（GitHub Actions等）の導入

## 8. P3一覧（将来改善）

- Stripe、Googleログイン、Googleカレンダー同期、本番プッシュ通知、複数人チーム機能、高度な分析（`CLAUDE.md` 9章と同一方針）

---

## 9. できている機能一覧

- 人脈カード一覧の表示・検索（名前/業種/メモ）・分類フィルタ・業種フィルタ・並び替え（実データ連動、`HomeScreen.tsx`のfilteredPeopleロジック）
- 人物詳細画面の表示、追加メモの保存（`PersonDetailScreen.tsx`、AsyncStorageへ実際に永続化）
- 次回連絡日時のローカル通知の設定・キャンセル（`notificationService.ts`、`expo-notifications`を正しく利用。実機で機能する数少ない完成度の高い機能）
- 予定前ナビ／後メモ／文面確認の**ライブ実装**（`HomeScreen.tsx`内の`PreMeetingPane`/`AfterMemoPane`/`LineCheckPane`）は、正規表現ベースのローカル・ヒューリスティック（`inferPain`/`inferTemperature`/`inferLineIssue`等）で入力内容に応じた分岐を行い、**実際にAsyncStorageへ人脈カードを更新保存する**（`savePeople`呼び出しを確認済み）。「モックAI」ではあるが、入力を無視する`createMockAnalysis`とは異なり一定の実体を持つ。
- TypeScript型チェックはエラーなしで通過

## 10. できていない機能一覧

- 新規人物追加（導線が死んでいる。上記1.1）
- 人物アーカイブ・削除
- Supabase・クラウド同期・再ログイン復元（ログイン自体が存在しない）
- 実AI（LLM）呼び出し
- 営業コーチのマルチターン会話・履歴保存
- 終業後チェックの実データ連動
- モバイルQR/Cloudflare Tunnel/betaプロキシ運用
- 法務・プライバシー・AI注意ページ
- テスト・CI

## 11. 見た目だけで実体が弱い機能

| 機能 | 実体 |
|---|---|
| 人物追加時の「AIで整理する」 | 入力メモを無視し固定値を返す（`mockAnalysis.ts:3`） |
| 予定前ナビの「予定前ナビを保存」「履歴を見る」「参照情報を編集」 | `Alert.alert`のみ、実処理なし |
| 文面確認の「履歴」「通知設定」 | `Alert.alert`のみ |
| 終業後チェック画面全体 | 完全固定文字列。`people`未接続。「村上さん」は架空 |
| 営業コーチ | 単発回答のみ、入力内容の反映は最小限、会話継続不可 |
| PersonCardの「詳細/LINE文/通知」ボタン | `View`のみ、タップ無反応 |
| Homeの優先行動カードの「詳細/完了/延期」ボタン | `View`のみ、タップ無反応（カード全体タップで人物詳細に飛ぶのみ） |
| `src/screens/PreMeetingNavScreen.tsx`ほか3画面 | ナビゲーションスタックに登録されているが、どこからも遷移されない死んだコード（513行） |

---

## 12. 新規人物E2E結果

**FAIL（ステップ1/29で停止）。**

実施内容: ブラウザでWeb版アプリを操作し、ホーム→人脈カードタブへ遷移。ヘッダーの人物追加風アイコン、人脈カード一覧全体をスクロールして「人物を追加する」ボタンの有無を確認。存在しないことを確認後、直接URL（`/AddPerson`）遷移を試行したがHomeへリダイレクト。

- ステップ1「UIから新規人物を追加」: **失敗**（導線なし）
- ステップ2以降: 前提が満たせないため実行不能

これは`CLAUDE.md`の「最上位ルール」（新規Contactでの動作確認）が、現状のビルドでは原理的に検証不可能であることを意味する。

## 13. Supabase実データ確認結果

対象なし。Supabaseへの依存関係・接続コード・テーブル定義がリポジトリ内に一切存在しない。`CLAUDE.md`記載の16テーブルはすべて未着手。

## 14. AIContext確認結果

該当する概念（Contact単位でAIへ渡す文脈オブジェクト）はコード上に存在しない。AIロジックはすべて`HomeScreen.tsx`内のヘルパー関数に直書きされ、呼び出し時にその場で`Person`オブジェクトの一部フィールドを文字列補間しているのみ。`AIContext`型・生成ロジックともに未実装。

## 15. AIモデル差し替え可能性の確認

**不可能。** `CLAUDE.md` 4.4が要求する「UI→logic→LLM Adapter→provider」の構造は存在せず、AI相当の処理（`createMockAnalysis`, `createLineCheckAnalysis`, `createAfterMemoSuggestion`, `createCoachMockAnswer`等）はすべて同期的なローカル関数として画面コンポーネント内に直書きされている。ネットワーク呼び出しは1件も存在しない（`fetch`/`axios`等の使用箇所なし）。モデルを差し替えるには、これらの関数をゼロから非同期Adapter経由の呼び出しに書き換える必要がある。

## 16. セキュリティ確認結果

- バックエンドが存在しないため、RLS・user_id分離・anonアクセス・service role key漏洩などの評価対象が存在しない。
- リポジトリ内に`.env`ファイル、認証情報、トンネルURLの類は確認されず、`.gitignore`も一般的な内容で妥当。
- 現時点で「壊れているセキュリティ」はないが、「セキュリティを実装すべき対象（バックエンド）がまだ存在しない」状態。

## 17. Auth/アカウント運用確認結果

未実装。ログイン・サインアップ・パスワードリセット・セッション切れ・アカウント削除、いずれの画面・ロジックも存在しない。

## 18. スマホQR確認結果

- Expo標準の`npx expo start`によるExpo Go QR起動は`README.md`記載の通り機能する（コード変更不要な標準機能）。
- 一方、`CLAUDE.md` 8章が前提とする「Cloudflare Quick Tunnel → beta proxy → /api/ollama proxy → Ollama → Supabase」という経路は、そのための設定ファイル・プロキシコード・ドキュメントがリポジトリ内に一切存在しない。この節はまだ構築されていないインフラを前提として書かれている。

## 19. エラー処理確認結果

- 通知許可拒否時のエラー表示は実装されている（`PersonDetailScreen.tsx`のtry/catch + Alert、数少ない実エラーハンドリング）。
- `personStorage.ts`の`getPeople`はJSON.parse失敗時に空配列へフォールバックする最小限の防御あり。
- それ以外（AI処理・保存処理）は同期的なローカル処理のため失敗しうるケースがほぼなく、ローディング表示・失敗表示の設計自体が未検証。将来LLM/Supabaseの非同期呼び出しを導入した瞬間に、ローディング状態・失敗時表示・二重送信防止のすべてを新規に設計する必要がある。

## 20. 運用/監視/バックアップ確認結果

対象となる本番基盤が存在しないため評価対象なし。

## 21. 法務/プライバシー/AI注意確認結果

利用規約・プライバシーポリシー・AI利用注意・問い合わせ導線、いずれも存在しない。実ユーザーに触らせる計画がある場合はP1として扱う必要がある。

## 22. テスト/回帰不足

- 自動テストなし、lintスクリプトなし、CI設定（`.github/workflows`）なし。
- 唯一の自動検証は`npx tsc --noEmit`（本監査で実行し、エラー0件を確認）。
- 新規人物E2E・RLS回帰・AI失敗時DB未保存・アーカイブ後非表示など、`CLAUDE.md`第12章が求める回帰テストは1件も存在しない。

---

## 23〜24. 完成までに切るべきGitHub Issues一覧（目的・受け入れ条件つき）

### Issue A（P0）: 新規人物追加の導線を復旧する
- **背景**: `PeoplePane`の追加ボタンが`ListEmptyComponent`にしか存在せず、モック3件が常駐するため到達不可能（`src/screens/HomeScreen.tsx:213,465-473`）。
- **対象画面**: ホーム（人脈タブ）、ヘッダー
- **実装内容**: ヘッダーの人物追加アイコン（現在は`onAdd`の汎用スタブに誤配線）を人脈タブ表示中は`navigation.navigate('AddPerson')`に配線する。または人脈タブ常設のFAB（フローティング追加ボタン）を新設する。
- **受け入れ条件**: 人脈カードが1件以上ある状態でも、UI操作のみで`AddPersonScreen`に到達できる。
- **確認方法**: 実機/ブラウザでの手動操作。人物を1人追加し人脈カード一覧に反映されることを確認。

### Issue B（P0）: ホームの優先行動を実データ駆動にする
- **背景**: `createTodayActions`が山本/田中/佐藤の固定名検索（`src/screens/HomeScreen.tsx:2363-2406`）。
- **対象画面**: ホーム
- **対象テーブル**: 将来的に`contacts`/`action_tasks`（現状はAsyncStorageの`Person[]`）
- **実装内容**: 次回連絡日・優先度スコア（`priorityScore`は既存）に基づき、全人物から動的に優先行動リストを生成するよう書き換える。
- **受け入れ条件**: 新規追加した人物が、条件（次回連絡日が近い等）を満たせば「今日の優先行動」に表示される。
- **確認方法**: 新規人物を追加し次回連絡日を明日に設定、ホームに表示されることを確認。

### Issue C（P0/P1境界）: 「AIで整理する」を入力内容に応じた出力に変更する
- **背景**: `createMockAnalysis`が引数`_memo`を無視（`src/data/mockAnalysis.ts:3`）。
- **対象画面**: 人物追加
- **実装内容**: 短期的には入力メモの正規表現ヒューリスティック（既存の`inferPain`等と同様の手法）で分類・スコアを変化させる。中期的にはIssue Fで導入するLLM Adapter経由の実AI呼び出しに置き換える。
- **受け入れ条件**: 異なる内容のメモを入力すると異なる分析結果が返る。
- **確認方法**: 業種の異なる2件のメモを入力し、分析結果（業種・分類・スコア）が異なることを確認。

### Issue D（P1）: 終業後チェックを実データ連動にする
- **背景**: `EndOfDayPane`が`people`を受け取らず全項目固定文字列（`src/screens/HomeScreen.tsx:1496-1767`）。
- **対象画面**: 終業後チェック
- **実装内容**: `people`をpropsで受け取り、後メモ未入力・次回連絡日未設定などを実データから集計する。
- **受け入れ条件**: 実際の人脈データの状態（後メモ有無等）が終業後チェックの集計結果に反映される。
- **確認方法**: 後メモ未入力の人物を1人作り、「未処理・漏れ」に表示されることを確認。

### Issue E（P1）: 死んだ画面の整理（重複実装の解消）
- **背景**: `src/screens/PreMeetingNavScreen.tsx`/`AfterMemoScreen.tsx`/`LineCheckScreen.tsx`/`EndOfDayCheckScreen.tsx`（513行）がナビゲーションスタックに登録されているがどこからも到達しない。実体は`HomeScreen.tsx`内のPane。
- **対象画面**: 上記4画面、`RootNavigator.tsx`
- **実装内容**: 到達経路のない4画面を削除するか、`HomeScreen.tsx`のPaneをこちらに統合して二重実装を解消する。
- **受け入れ条件**: 同一機能の実装が1箇所のみになる。`RootNavigator`に不要なスタック登録が残らない。
- **確認方法**: コードレビュー＋`npx tsc --noEmit`。

### Issue F（P1）: LLM Adapter層の新設
- **背景**: `CLAUDE.md` 4.4のUI/logic/Adapter/provider分離が存在せず、AIロジックが画面コンポーネントに直書き。
- **対象画面**: 人物追加、予定前ナビ、後メモ、文面確認、営業コーチ、終業後チェック
- **実装内容**: `src/ai/llmAdapter.ts`相当を新設し、入出力型を固定。第一実装はOllamaローカルモデルを想定。
- **受け入れ条件**: UIコンポーネントがAdapter経由でのみAI結果を取得する。AI失敗時はDB（ローカル保存）を更新しない。
- **確認方法**: Adapterをモック実装に差し替えても画面が動作することを確認。

### Issue G（P1）: Supabaseスキーマ導入と永続化移行
- **背景**: 永続化がAsyncStorageのみで、クラウド同期・再ログイン復元が不可能。
- **対象テーブル**: `CLAUDE.md` 4.2記載の16テーブル
- **実装内容**: スキーマ作成、RLSポリシー設定、`personStorage.ts`相当のリポジトリ層をSupabaseクライアント経由に置き換え。
- **受け入れ条件**: 新規人物データがSupabaseに実際に保存され、別セッションでも復元される。
- **確認方法**: Supabaseダッシュボードで実データ確認。

### Issue H（P1）: 認証導入
- **背景**: ログイン機能が存在しない。
- **実装内容**: サインアップ・ログイン・パスワードリセット・セッション復元。
- **受け入れ条件**: ログアウト後再ログインでデータが復元される。
- **確認方法**: 手動E2E。

### Issue I（P1）: 人物アーカイブ・削除機能
- **背景**: `CLAUDE.md` 5.9で要求されているが未実装。Issue Aの代替手段としても機能しうる。
- **実装内容**: 人物詳細からのアーカイブ、確認モーダル、一覧・検索・ピッカーからの除外。
- **受け入れ条件**: アーカイブ後、通常一覧に表示されない。履歴データは保持される。
- **確認方法**: 手動確認。

### Issue J（P2）: Alertスタブの実装または削除
- **背景**: 27箇所の`Alert.alert('...想定です')`が確認された。
- **実装内容**: 各スタブについて実装するか、UIから削除するかを個別判断。
- **受け入れ条件**: 「想定です」という文言がコード上から消える。
- **確認方法**: `grep -rn "想定です" src`が0件になること。

### Issue K（P2）: 装飾のみのボタンの実装または削除
- **背景**: `PersonCard`の詳細/LINE文/通知、Homeの詳細/完了/延期が`View`のみで無反応。
- **実装内容**: 実際のアクション（詳細＝人物詳細へ遷移等）を実装するか、誤解を招くため削除する。
- **受け入れ条件**: 表示されているボタンはすべてタップに反応する。

### Issue L（P2）: HomeScreen.tsxの分割
- **背景**: 単一ファイルが2,941行。
- **実装内容**: `HomePane`/`PeoplePane`/`PreMeetingPane`/`AfterMemoPane`/`LineCheckPane`/`EndOfDayPane`と各ヘルパー関数をファイル分割。
- **受け入れ条件**: 各ファイルが管理しやすい単位に分かれる。`npx tsc --noEmit`が通る。

### Issue M（P1）: 新規人物E2E回帰チェックリストの整備
- **背景**: 自動・手動を問わずE2E回帰の仕組みがない。
- **実装内容**: `CLAUDE.md` 7章の28ステップを手動チェックリスト化し、`docs/`に配置。可能なら一部をE2Eテスト化。
- **受け入れ条件**: Issue A完了後、このチェックリストを最後まで実行できる。

### Issue N（P1、外部提供前提の場合のみ）: 最低限の法務ページ
- **背景**: 利用規約・プライバシー・AI注意が皆無。
- **実装内容**: β版向けの簡易ページを追加。
- **受け入れ条件**: アプリ内から到達可能。

---

## 25. 「これを全部潰せば完成」と言える最終チェックリスト

1. [ ] Issue A: 人物追加の導線が常に到達可能
2. [ ] Issue B: ホーム優先行動が新規人物にも反応する
3. [ ] Issue C: AI整理が入力内容に応じて変化する
4. [ ] Issue D: 終業後チェックが実データ連動
5. [ ] Issue E: 死んだ画面（4ファイル513行）の整理完了
6. [ ] Issue F: LLM Adapter層が存在し、UIが直接プロバイダを呼ばない
7. [ ] Issue G: Supabaseに実データが保存され、テーブル・RLSが機能
8. [ ] Issue H: ログイン・ログアウト・再ログイン復元が機能
9. [ ] Issue I: 人物アーカイブ・削除が機能
10. [ ] Issue J・K: スタブ・無反応ボタンが一掃されている
11. [ ] Issue M: 新規人物E2E（28ステップ）が最後まで手動で通過する
12. [ ] Issue N: 実ユーザー提供前提なら法務ページが存在する
13. [ ] `npx tsc --noEmit` が引き続きエラー0件
14. [ ] `grep -rn "想定です" src` が0件

上記すべてが完了して初めて、`CLAUDE.md`が定義する「完成」を主張できる。**現時点での最優先着手はIssue A（新規人物追加導線の復旧）である。これが直らない限り、他のいかなる改善も「新規人物での動作確認」という本Issueの主題を満たせない。**

---

## 型チェック結果

`npx tsc --noEmit`: **success**（エラー0件）
